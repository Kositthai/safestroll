import { StyleSheet } from 'react-native';

export const form = StyleSheet.create({
  input20: {
    color: 'white',
    borderStyle: 'solid',
    borderWidth: 2,
    borderRadius: 10,
    borderColor: 'white',
    marginBottom: 10,
    width: '100%',
    height: '20%',
    padding: 10,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
