import { StyleSheet } from 'react-native';

export const form = StyleSheet.create({
  input20: {
    color: 'white',
    borderStyle: 'solid',
    borderWidth: 2,
    borderRadius: 10,
    borderColor: 'white',
    width: 300,
    padding: 15,
    marginBottom: 10,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
